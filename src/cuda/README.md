# 🎮 Código CUDA — GEMM em GPU

Arquivo principal:

```text
gemm_cuda.cu
```

Este arquivo contém o código-base em CUDA para as versões GPU do trabalho.

## Kernels pendentes

- `gemm_cuda_naive`
- `gemm_cuda_tiled`

## Compilação prevista

```bash
nvcc gemm_cuda.cu -O3 -o gemm_cuda
```
