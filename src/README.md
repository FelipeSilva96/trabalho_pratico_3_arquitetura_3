# 📂 Pasta `src/`

Esta pasta contém os códigos-base extraídos do notebook do enunciado.

## Estrutura

```text
src/
├── cpu/
│   └── gemm_cpu.c
└── cuda/
    └── gemm_cuda.cu
```

## Observação

Os arquivos ainda possuem `TODOs` e servem apenas como ponto de partida. A implementação efetiva será feita posteriormente.
